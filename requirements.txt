Django==6.0
psycopg2-binary>=2.9.3
# fastembed (ONNX Runtime) runs the same all-MiniLM-L6-v2 model as
# sentence-transformers did, without needing torch at all - much lighter
# on a memory-constrained instance, and verified numerically equivalent
# (see jobs/services/embeddings.py).
fastembed>=0.3.0
numpy>=1.24.0

# Production dependencies
gunicorn>=21.2.0
whitenoise>=6.6.0
python-decouple>=3.8
dj-database-url>=2.1.0
