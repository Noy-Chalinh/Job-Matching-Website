Django==6.0
psycopg2-binary>=2.9.3
# fastembed (ONNX Runtime) runs the same BAAI/bge-small-en-v1.5 model as
# sentence-transformers does, without needing torch at all - much lighter
# on a memory-constrained instance (see jobs/services/embeddings.py).
fastembed>=0.3.0
numpy>=1.24.0

# pgvector: Django VectorField/CosineDistance/HnswIndex for JobEmbedding
# (see jobs/models.py). Pure-Python, no native/torch dependency - safe to
# install on the web dyno unlike requirements-backfill.txt's torch.
pgvector>=0.3.6

# Production dependencies
gunicorn>=21.2.0
whitenoise>=6.6.0
python-decouple>=3.8
dj-database-url>=2.1.0
